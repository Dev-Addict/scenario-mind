import React from "react";
import {Link} from "react-router-dom";

const Result = () => {
    return (
        <div className="route">
            <Link to="/" className="button">شروع از اول</Link>
        </div>
    );
};

export default Result;
